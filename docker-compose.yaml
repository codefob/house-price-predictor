services: 
  fastapi: 
    image: codefob/fastapi:dev  
    build: 
      context: .
      dockerfile: Dockerfile
    ports: 
      - 8889:8000

  streamlit: 
    image: codefob/streamlit:dev
    build:
      context: ./streamlit_app
      dockerfile: Dockerfile
    ports:
      - 8501:8501
    environment: 
      API_URL: http://fastapi:8000 

